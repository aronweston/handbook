##Command Line Basics

```
$ mkdir folder (make a directory or sub directory folder)
$ touch index.html (create file in a directory)
$ rm index.html (remove file in a directory)
$ cd .. (move back to directory)
$ cd rm -r folder (remove folder)
$ ls -a (list all documents, hidden and non-hidden in a directory)


```